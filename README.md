# MiniLab-Hand

A lightweight project for dexterous hand reinforcement learning and simulation, reconstructed step-by-step from MiniLab.

## Development Log

- **[2026-06-13] Step 1: Scaffold Initialization & Dependency Audit**
  - **Purpose**: Initialize project folder structure, configuration files, and audit dependencies.
  - **Verification**: Created `tests/01_test_scaffold.py` to check import of `torch`, `mujoco`, `gymnasium`, `numpy` and verify MPS/GPU backend availability and basic XML compilation.
